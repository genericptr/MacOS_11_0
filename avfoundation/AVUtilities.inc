{ Parsed from AVFoundation.framework AVUtilities.h }

{$if defined(__NULL_MACRO__)}
{$ifdef FUNCTIONS}
function AVMakeRectWithAspectRatioInsideRect(aspectRatio: CGSize; boundingRect: CGRect): CGRect; cdecl; external; { unavailable in watchos } { available in macos 10.7, ios 4.0, tvos 9.0 }
{$endif}

{$else}
{$endif}
